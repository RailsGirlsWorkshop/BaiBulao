<p id="notice"><%= notice %></p>

<h1>Bais</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bais.each do |bai| %>
      <tr>
	  <td><%= bai.name %></td>
        <td><%= link_to 'Show', bai %>

</td>
        <td><%= link_to 'Edit', edit_bai_path(bai) %></td>
        <td><%= link_to 'Destroy', bai, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Bai', new_bai_path %>
